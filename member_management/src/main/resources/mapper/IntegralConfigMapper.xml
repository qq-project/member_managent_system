<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qiqi.member_management.management.business.mapper.IntegralConfigMapper">
  <resultMap id="BaseResultMap" type="com.qiqi.member_management.management.business.model.IntegralConfig">
    <id column="id" jdbcType="INTEGER" property="id" />
    <result column="integral_id" jdbcType="VARCHAR" property="integralId" />
    <result column="integral_min_amount" jdbcType="INTEGER" property="integralMinAmount" />
    <result column="integral_max_amount" jdbcType="INTEGER" property="integralMaxAmount" />
    <result column="level" jdbcType="INTEGER" property="level" />
    <result column="discount" jdbcType="DECIMAL" property="discount" />
    <result column="added_by_name" jdbcType="VARCHAR" property="addedByName" />
    <result column="added_time" jdbcType="TIMESTAMP" property="addedTime" />
    <result column="last_modified_by_name" jdbcType="VARCHAR" property="lastModifiedByName" />
    <result column="last_modified_time" jdbcType="TIMESTAMP" property="lastModifiedTime" />
    <result column="valid" jdbcType="CHAR" property="valid" />
    <result column="rate" jdbcType="DECIMAL" property="rate" />
  </resultMap>
  <delete id="deleteByPrimaryKey" parameterType="java.lang.Integer">
    delete from t_integral_config
    where id = #{id,jdbcType=INTEGER}
  </delete>
  <insert id="insert" parameterType="com.qiqi.member_management.management.business.model.IntegralConfig">
    <selectKey keyProperty="id" order="AFTER" resultType="java.lang.Integer">
      SELECT LAST_INSERT_ID()
    </selectKey>
    insert into t_integral_config (integral_id, integral_min_amount, integral_max_amount, 
      level, discount, added_by_name, 
      added_time, last_modified_by_name, last_modified_time, 
      valid,rate)
    values (#{integralId,jdbcType=VARCHAR}, #{integralMinAmount,jdbcType=INTEGER}, #{integralMaxAmount,jdbcType=INTEGER}, 
      #{level,jdbcType=INTEGER}, #{discount,jdbcType=DECIMAL}, #{addedByName,jdbcType=VARCHAR}, 
      #{addedTime,jdbcType=TIMESTAMP}, #{lastModifiedByName,jdbcType=VARCHAR}, #{lastModifiedTime,jdbcType=TIMESTAMP}, 
      #{valid,jdbcType=CHAR},#{rate,jdbcType=DECIMAL})
  </insert>
  <update id="updateByPrimaryKey" parameterType="com.qiqi.member_management.management.business.model.IntegralConfig">
    update t_integral_config
    set integral_id = #{integralId,jdbcType=VARCHAR},
      integral_min_amount = #{integralMinAmount,jdbcType=INTEGER},
      integral_max_amount = #{integralMaxAmount,jdbcType=INTEGER},
      level = #{level,jdbcType=INTEGER},
      discount = #{discount,jdbcType=DECIMAL},
      added_by_name = #{addedByName,jdbcType=VARCHAR},
      added_time = #{addedTime,jdbcType=TIMESTAMP},
      last_modified_by_name = #{lastModifiedByName,jdbcType=VARCHAR},
      last_modified_time = #{lastModifiedTime,jdbcType=TIMESTAMP},
      valid = #{valid,jdbcType=CHAR},
      rate = #{rate,jdbcType=DECIMAL}
    where id = #{id,jdbcType=INTEGER}
  </update>
  <select id="selectByPrimaryKey" parameterType="java.lang.Integer" resultMap="BaseResultMap">
    select id, integral_id, integral_min_amount, integral_max_amount, level, discount, 
    added_by_name, added_time, last_modified_by_name, last_modified_time, valid, rate
    from t_integral_config
    where id = #{id,jdbcType=INTEGER}
  </select>
  <select id="selectAll" resultMap="BaseResultMap">
    select id, integral_id, integral_min_amount, integral_max_amount, level, discount, 
    added_by_name, added_time, last_modified_by_name, last_modified_time, valid, rate
    from t_integral_config
    where valid = 'T'
  </select>
  <!--queryIntegralConfig-->
    <select id="queryIntegralConfig"
            resultType="com.qiqi.member_management.management.business.model.IntegralConfig"
            parameterType="java.lang.Integer">
      select id, integral_id, integral_min_amount, integral_max_amount, level, discount,
      added_by_name, added_time, last_modified_by_name, last_modified_time, valid, rate
      from  t_integral_config
      where valid = 'T'
      <if test="null != integral and integral > 0">
        and integral_min_amount <![CDATA[ <= ]]> #{integral, jdbcType=INTEGER}
      </if>
      <if test="null != integral and integral > 0">
        and integral_max_amount <![CDATA[ >= ]]> #{integral, jdbcType=INTEGER}
      </if>
      order by id DESC
      limit 1
    </select>

  <!--更新操作-->
  <update id="updateIntegralConfig" parameterType="com.qiqi.member_management.management.business.model.IntegralConfig">
    update t_integral_config
    <set>
        <if test="null != discount">
          discount = #{discount, jdbcType=DECIMAL},
        </if>
        <if test="null != rate">
          rate = #{rate, jdbcType=DECIMAL},
        </if>
        <if test="null != integralMinAmount">
          integral_min_amount = #{integralMinAmount, jdbcType=DECIMAL},
        </if>
        <if test="null != integralMaxAmount">
          integral_max_amount = #{integralMaxAmount, jdbcType=DECIMAL},
        </if>
      last_modified_by_name = 'qiqi',
      last_modified_time = NOW(3),
    </set>
    where valid = 'T' and id = #{id, jdbcType=VARCHAR}
    
  </update>

  <select id="getIntegralMaxAmount"
          resultType="com.qiqi.member_management.management.business.vo.IntegralAmountVo">
    select max(integral_max_amount) integralMaxAmount, min(discount) discount
    from  t_integral_config
  </select>

  <insert id="saveIntegralConfigRequest" parameterType="com.qiqi.member_management.management.business.model.IntegralConfig">
    <selectKey keyProperty="id" order="AFTER" resultType="java.lang.Integer">
      SELECT LAST_INSERT_ID()
    </selectKey>
    insert into t_integral_config
    <trim prefix="(" suffix=")" suffixOverrides=",">
      <if test="null != integralMinAmount and '' != integralMinAmount">
        integral_min_amount,
      </if>
      <if test="null != integralMaxAmount and '' != integralMaxAmount">
        integral_max_amount,
      </if>
      <if test="null != discount and '' != discount">
        discount,
      </if>
      <if test="null != rate and '' != rate">
        rate,
      </if>
      added_by_name,
      added_time,
      last_modified_by_name,
      last_modified_time,
      valid
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides=",">
      <if test="null != integralMinAmount">
        #{integralMinAmount, jdbcType=DECIMAL},
      </if>
      <if test="null != integralMaxAmount">
        #{integralMaxAmount, jdbcType=DECIMAL},
      </if>
      <if test="null != discount ">
        #{discount, jdbcType=DECIMAL},
      </if>
      <if test="null != rate">
        #{rate, jdbcType=DECIMAL},
      </if>
      'qiqi',
      NOW(3),
      'qiqi',
      NOW(3),
      'T'
    </trim>
  </insert>
   <select id="getLastIntegralConfig" resultMap="BaseResultMap">
     select  id, integral_id, integral_min_amount, integral_max_amount, level, discount,
      added_by_name, added_time, last_modified_by_name, last_modified_time, valid, rate
      from  t_integral_config
      order by id DESC
      limit 1
   </select>
</mapper>